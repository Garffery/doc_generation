DRAFT_REPORT_PROMPT = """您将收到一份**需求拆解简报**（含功能点列表）。请据此撰写一份**后端开发技术文档草稿**，供后端研发评审与实现参考。

{skills_section}

<需求拆解简报>
{research_brief}
</需求拆解简报>

重要提示：请确保文档与用户输入语言一致！
若需求拆解简报为中文，则全文用中文；若为英文，则全文用英文。

今天的日期是 {date}。

您需要输出完整的**后端开发技术文档草稿**（将写入 `draft_report` 字段）。文档应在需求拆解基础上补充合理的技术设计，但须与简报中的功能点一一对应，不得遗漏或擅自合并功能点。

---

## 文档定位

- **读者对象**：后端开发人员；文档聚焦服务端实现、协议与数据，不写测试用例、测试要点或 QA 检查清单。
- 这是**技术文档草稿**：可包含接口、数据模型、流程等技术内容，但应标明「待确认」处，对简报中未说明的细节不做武断定论。
- **以上游功能点为主干**：简报中的每个功能点（F-001、F-002…）至少对应文档中的一个独立章节。
- **不写成研究报告**：不需要文献综述、来源引用、参考文献列表或外部 URL 引用。

---

## 推荐文档结构（必须按此顺序组织，使用 Markdown）

### # 文档标题
基于需求摘要拟定，体现系统/模块名称。

### ## 1. 概述
- 背景与目标（来自需求摘要，可略作技术视角补充）
- 文档范围与读者对象（明确为后端开发）
- 术语表（若有多义业务名词）

### ## 2. 系统架构（草稿级）
- 逻辑架构：主要服务/模块划分及职责
- 与外部系统的集成关系（仅列出简报中已提及或拆解中依赖的系统）
- 关键技术假设（如 Socket 通信、关系型数据库等）— 若简报未指定，用「建议/待选型」表述，勿写成已定事实
- **前后端交互方式**：客户端与服务端之间**不采用 HTTP/REST** 作为主要接口，而是通过 **Socket**（如 WebSocket/TCP，按项目实际）收发二进制协议包；外部系统对接若涉及 HTTP，单独说明，勿与游戏内前后端协议混淆

### ## 3. 功能点技术设计（核心，按 F-xxx 逐节展开）

对简报中**每一个**功能点单独设节，标题格式：`### 3.x F-xxx：<功能点名称>`（编号与简报保持一致）。

每节建议包含（无信息则写「待澄清」，勿编造）：

| 小节 | 内容 |
|------|------|
| 需求回顾 | 从简报摘录：描述、范围、业务规则 |
| 接口设计（草稿） | **Socket 协议**：协议段/消息号、C2S 与 S2C 方向、载荷字段（类型与位宽，表格）、触发场景与交互时序；勿写 HTTP 方法/URL。错误码或失败回包要点（有依据时写） |
| 领域逻辑 | 核心处理流程、状态机、校验与权限 |
| 数据设计（草稿） | 主要实体/表、关键字段、索引或唯一约束建议 |
| 依赖与调用 | 依赖的其他 F-xxx、外部服务、消息/事件（如有） |
| 异常与边界 | 失败场景、幂等、并发、限流等（仅在有依据时写） |
| 待澄清 | 合并简报与本节设计仍不确定的项 |

若某功能点简报标注为「待澄清」较多，本节以需求回顾 + 待澄清 + 最小可行设计建议为主，避免虚构细节。

### ## 4. 功能点依赖与实施顺序
- 根据简报「功能点关系」整理开发/联调顺序
- 标注可并行项与关键路径

### ## 5. 非功能需求（草稿）
- 仅展开简报「全局约束」中已提及的项（性能、安全、可用性、日志、监控等）
- 未提及则写「需求中未说明，实施前需确认」

### ## 6. 风险、假设与开放问题
- 汇总各功能点待澄清项及全局风险
- 列出文档撰写时采用的假设（须可替换）

### ## 7. 附录（可选）
- 统一错误码约定、鉴权方式草案、枚举定义等跨功能点内容

---

## 撰写原则

1. **忠实于需求拆解**：不得删除、合并或重命名简报中的功能点；新增技术内容不得与简报中的范围、规则冲突。
2. **草稿诚实**：对简报未给出的接口字段、表名、技术栈，用「建议」「示例」「待确认」标注，不要冒充用户需求。
3. **可实施导向**：开发人员应能根据本文档理解各功能点的 **Socket 协议形态**、数据落点与联调关系；细节可在评审后迭代。前后端联调以消息号与二进制字段为准，不以 REST API 描述。
4. **结构清晰**：使用 Markdown（`#` / `##` / `###`）；表格、列表、代码块（协议字段表、Erlang `pack`/`read` 片段或 SQL 示例）适度使用；接口示例优先协议表，避免用 JSON REST 示例代替 Socket 载荷。
5. **专业客观**：不使用第一人称；不写「我将…」「本报告研究了…」等研究报告口吻。
6. **篇幅**：功能点章节应充实但避免重复粘贴简报原文；需求回顾用摘要即可，重点放在技术设计草稿。

---

切记：
- 输出的是**后端开发技术文档草稿**，不是深度研究报告，也不包含测试要点、测试用例或 QA 清单。
- 每个 F-xxx 功能点必须有对应技术设计章节。
- 全文语言与需求拆解简报一致。
"""
