DRAFT_REPORT_PROMPT = """您将收到一份**需求拆解简报**（含功能点列表）。请据此撰写一份**后端开发技术文档草稿**，作为最终文档的修改的依据。

{skills_section}

<需求拆解简报>
{research_brief}
</需求拆解简报>

重要提示：请确保文档与用户输入语言一致！
若需求拆解简报为中文，则全文用中文；若为英文，则全文用英文。

今天的日期是 {date}。

您需要输出完整的**后端开发技术文档草稿**（将写入 `draft_report` 字段）。文档应在需求拆解基础上补充合理的技术设计，但须与简报中的功能点一一对应，不得遗漏或擅自合并功能点。

---

## 文档定位

- **读者对象**：后端开发人员；文档聚焦服务端实现、协议与数据，不写测试用例、测试要点或 QA 检查清单。
- 这是**技术文档草稿**：可包含接口、数据模型、流程等技术内容，但应标明「待确认」处，对简报中未说明的细节不做武断定论。
- **以上游功能点为主干**：简报中的每个功能点（F-001、F-002…）至少对应文档中的一个独立章节。

---

## 推荐文档结构（必须按此顺序组织，使用 Markdown）

### # 文档标题
基于需求摘要拟定，体现系统/模块名称。

### ## 1. 玩法概述
- 玩法说明
- 玩法的核心规则

### ## 2. 数据持久化（草稿级）
- 数据持久化方案：分析需要实现的需求,设计数据持久化的方案。
- 需要说明存储数据的含义
例如:
- player_misc_extra方案
    value:存储玩家单局最高积分
    value_text:[{{plv,玩家等级}},{{gift_list,礼包列表}}]
- role_data方案
    #role.normal_priority{{id=>#{{buy_time=>购买时间}}}},其中id是特权id
    
    
### ## 3.配置表设计(草稿)
- 根据需求拆解简报,设计满足功能的配置的字段,设计的配置表字段应该遵循下面的规范
|字段|类型(erlang数据类型)|说明|示例|



### ## 3. 功能点技术设计（核心，按 F-xxx 逐节展开）

对简报中**每一个**功能点单独设节，标题格式：`### 3.x F-xxx：<功能点名称>`（编号与简报保持一致）。

每节建议包含（无信息则写「待澄清」，勿编造）：

| 小节 | 内容 |
|------|------|
| 需求回顾 | 从简报摘录：描述、范围、业务规则 |
| 功能逻辑 | 该功能的说明,核心流程说明 |
| 接口设计（草稿） |如果接口类型是外部交互接口在,则需要根据需求设计提供相应C2S 与 S2C字段,暂时不需要指明协议号|
| 配置表设计 | 如果实现需要读取配置,可以根据功能进行配置表字段设计|
| 依赖与调用 | 依赖的其他 F-xxx、外部服务、消息/事件（如有） |

若某功能点简报标注为「待澄清」较多，本节以需求回顾 + 待澄清 + 最小可行设计建议为主，避免虚构细节。

### ## 4. 功能点依赖与实施顺序
- 根据简报「功能点关系」整理开发/联调顺序
- 标注可并行项与关键路径

### ## 5. 风险、假设与开放问题
- 汇总各功能点待澄清项
- 列出文档撰写时采用的假设（须可替换）

---

## 撰写原则

1. **忠实于需求拆解**：不得删除、合并或重命名简报中的功能点；新增技术内容不得与简报中的范围、规则冲突。
2. **可实施导向**：开发人员应能根据本文档理解各功能点的 **Socket 协议形态**、数据落点与联调关系；细节可在评审后迭代。前后端联调以消息号与二进制字段为准，不以 REST API 描述。
3. **结构清晰**：使用 Markdown（`#` / `##` / `###`）；表格、列表、代码块（协议字段表、Erlang `pack`/`read` 片段或 SQL 示例）适度使用；接口示例优先协议表，避免用 JSON REST 示例代替 Socket 载荷。
4. **篇幅**：功能点章节应充实但避免重复粘贴简报原文；需求回顾用摘要即可，重点放在技术设计草稿,在能够说明需求的前提下，尽可能的简洁。

---

切记：
- 输出的是**后端开发技术文档草稿**，不是深度研究报告，也不包含测试要点、测试用例或 QA 清单。
- 每个 F-xxx 功能点必须有对应技术设计章节。
- 全文语言与需求拆解简报一致。
"""
