RESEARCH_BRIEF_PROMPT = """您将收到一组您和用户之间迄今为止的交流信息。您的任务是**分析需求并将其拆解为单一功能点**，供后续步骤据此生成erlang后端开发文档草稿。

**本步骤不输出后端开发文档**（不写接口定义、数据库设计、部署方案等技术实现细节）。只产出清晰、结构化的需求拆解结果。

{skills_section}

您和用户之间迄今为止的交流信息如下：
<消息>
{messages}
</消息>

重要提示：请确保输出与用户输入使用**相同的语言**！
例如，用户用英文交流则全文用英文；用户用中文交流则全文用中文。

今天的日期是 {date}。

您需要输出一份**需求拆解简报**（将写入 `research_brief` 字段），核心是功能点列表，便于下游据此扩写为后端开发文档。

---

## 输出结构（必须按此顺序组织）

### 1. 需求摘要（简要）
- 2–4 句话概括：要解决什么问题、主要用户/场景、后端相关范围边界（包含/不包含）。
- 仅作上下文，不展开技术方案。

### 2. 需求分析
- **已确认**：用户明确说过的功能、规则、约束、集成对象等（逐条列出，保留用户原意与术语）。
- **待澄清**：对拆解或后续设计有影响的缺失信息；不要假装已确认。
- **合理推断**（如有）：从上下文可推出的项，每条标注「推断」并说明依据；能放入待澄清的优先放入待澄清。

### 3. 功能点拆解（核心，篇幅应占输出主体）

将需求拆分为**单一功能点**。每个功能点须满足：
- **单一职责**：一个可独立理解、可单独交给下游写成文档小节的后端相关能力（如「用户注册」「订单状态查询」）。
- **需求粒度**：描述「做什么、为什么」，而非「怎么做」；不写具体 API 路径、表结构、框架选型。
- **粒度适中**：过大则继续拆分；过小且无法单独描述价值则合并。

对每个功能点使用以下模板（编号从 F-001 递增）：

#### F-xxx：<功能点名称>
- **描述**：该功能点做什么、服务谁、解决什么业务问题
- **范围**：本功能点包含的行为；明确**不包含**什么（避免与相邻功能点重叠）
- **触发与交互**：用户或系统如何触发（如「用户提交表单」「定时同步」）；概念级输入/输出（业务对象或事件，非 JSON 字段）
- **业务规则**：校验、状态、权限、边界条件等（仅写需求中已明确或强相关的规则）
- **依赖**：依赖的其他功能点（写编号，如 F-001）或外部系统（仅名称与关系）
- **待澄清**（如有）：仅与本功能点相关的未决问题

若需求中未提及某类信息，写「未说明」或「待澄清」，**不要编造**。

### 4. 功能点关系
- **依赖顺序**：哪些功能点须先于哪些（用编号说明）。
- **可并行**：可独立推进、无强依赖的功能点编号。


---

## 撰写原则

1. **只做需求拆解**：输出供「生成后端开发文档草稿」使用，本身不是开发文档。
2. **忠于用户输入**：用户提供的细节必须进入对应功能点或「已确认」列表，不得遗漏或改写原意。
3. **避免无依据的假设**：不添加用户未提及的技术栈、接口形态或数据模型；不确定的一律进「待澄清」。
4. **面向后端相关需求**：聚焦服务端需承载的能力与规则；不写 UI 交互细节，除非直接影响后端职责划分。
5. **不写验收与测试**：不输出验收要点、测试检查项或 QA 清单；完成标准由下游开发文档与实现评审另行约定。
6. **功能点可复用**：每个功能点应足够完整，使下游能单独据此扩写为一节开发文档，而无需回头猜测业务意图。

---

切记：
- 输出的是**需求拆解简报**，重点是功能点列表（F-001、F-002…），不是后端开发文档。
- 功能点模板中**不要**包含验收要点、测试要点或验收标准类小节。
- 全文语言必须与消息历史中用户主要使用的语言一致。
"""
